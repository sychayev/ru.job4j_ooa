package ru.job4j.condition;

public class MultiMax {

//    public static int max(int first, int second, int third) {
//        String s = "All values are little than third";
//        int res1 = (first > second && second < third) && (first < third) ? third : Integer.parseInt(s);
//        int res2 = (second > first && third <third ) && (first < third) ? third : Integer.parseInt(s);
//        int res3 = (first > second && second < third) && (first < third) ? third : Integer.parseInt(s);
//        return;
//    }

}
